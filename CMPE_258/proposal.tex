\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{geometry}
\geometry{margin=1in}
\nocite{*}

\title{Extreme Learning Machines: \\Reimplementation and Analysis}
\author{Ethan Ho\\
Charles W. Davidson College of Engineering, San José State University}
\date{October 21, 2025}

\begin{document}

\maketitle

\begin{abstract}
This proposal aims to investigate the Extreme Learning Machine presented by Huang \cite{huang2006extreme}, featuring a single-hidden-layer feed-forward neural network (SLFN). The model's design focuses on the randomization of weights and biases in the input layer and a computational approach to those hyperparameters in the output layer, rather than iteratively. The original work demonstrates the various benefits of such a design, including impressive training times and competitive generalization performance. This project plans to (1) implement the algorithm, (2) evaluate its performance on similar datasets and benchmarks, (3) investigate the impact of different parameters in the new design, and (4) evaluate differences between ELMs and modern solutions. By the end of this project, we should expect to gain a deeper understanding of design choices for neural networks and the value that back propagation brings.
\end{abstract}

\section{Introduction}
When deciding on prediction models, feed-forward neural networks are often used in machine learning with their specialty in approximating nonlinear patterns. However, training traditional neural networks trained via gradient descent and backpropagation is often quite time-consuming and comes with additional challenges like local minima and divergence. Huang \cite{huang2006extreme} proposes a solution to these bottlenecks known as Extreme Learning Machine (ELM), which principles itself on a single hidden-layer network with randomly initialized weights. Despite being proposed in 2006, ELM remains relevant for exploring non-iterative learning paradigms and connections to random feature mappings. \\

Related works in single-layered neural networks include many, like the discussion on their various applications on real-world datasets, additional benchmarking, and controversies by Wang \cite{wang2022review}. Additionally, Huang \cite{huang2011extreme} later continues his work on ELMs and their variations of batch learning, ensemble methods, and more. ELM is unique in the fact that it utilizes a random initialization of weights combined with the speed of a computational output rather than an iterative learning process. This project proposal aims to dive deep into the implementation, validate and verify the results, and dissect the key design choices of the authors.

\section{Problem Statement}
The current challenges faced when using traditional neural networks with back propagation are many, but begin with the time it takes to tune the parameters of the hidden layers and output an approximation. Relying on back propagation would require an iterative process of a forward pass, calculating error and the negative gradient, and finally using the chain rule to update the previous neurons. But with poor learning rates, these methods may take increasingly longer to converge or become unstable and diverge completely. Additionally, the existence of local minima and their hurdle of hindering true convergence is another difficulty brought on by gradient descent. Lastly, the more generic issue of neural networks easily overfitting due to specialized neurons leads to less generic models. Although many of these issues have their own proposed solutions (e.g, early-stop, optimizers, and regularization), ELMs address them fundamentally through their analytical training process.

\section{Proposed Solution}
The algorithm that Huang \cite{huang2006extreme} focuses on and appropriately named Extreme Learning Machine is based on two key design choices: (1) a single-hidden-layer feed-forward neural network, and (2) replacing back propagation with analytically calculated weights and biases. The authors push to prove that with only one hidden layer with its weights and biases chosen at random, it is possible to calculate the output by solving the linear system. This approach brings with it a small training error while also improving the model's ability to generalize, making the need for modern accommodations for neural networks unnecessary. \\

As described by Huang \cite{huang2006extreme}, consider an SLFN with $N$ input samples $\{(x_i, t_i)\}_{i=1}^N$, $L$ hidden nodes, activation function $G(\cdot)$, hidden-node parameters $\{(a_j, b_j)\}_{j=1}^L$, (weights and biases), and output-weight matrix $\beta$. The network output can be written:
\begin{equation}
f(x) = \sum_{j=1}^{L} \beta_j G(a_j \cdot x_i + b_j)
\end{equation}

Because our model has only a single hidden layer, the authors express the output approximation in a clean equation with our input data $x$ in a linear form, passed through a non-linear activation function while being multiplied by the weight at the input, as well as the hidden layer represented as $w$ and $\beta$. \\

\noindent We can then define the output of our hidden layer matrix $H \in \mathbb{R}^{N \times L}$:
\begin{equation}
H_{ij} = G(a_j \cdot x_i + b_j)
\end{equation}

\noindent and $H$ as:
\begin{equation}
H = \begin{bmatrix}
G(w_1 \cdot x_1 + b_1) & \cdots & G(w_L \cdot x_1 + b_L) \\
\vdots & \ddots & \vdots \\
G(w_1 \cdot x_N + b_1) & \cdots & G(w_L \cdot x_N + b_L)
\end{bmatrix}
\end{equation}

\noindent The target matrix $T \in \mathbb{R}^{N \times m}$ can be defined as:
\begin{equation}
T = H \cdot \beta
\end{equation}

\noindent By solving for $\beta$, our vector of weights from the hidden layer to the output layer, we get:
\begin{equation}
\beta = H^+ T
\end{equation}
where $H^+$ is the Moore-Penrose pseudoinverse of $H$ \cite{serre2000matrices,rao1972generalized}. \\

Mathematically, if we have $N$ hidden nodes for the same $N$ number of test samples inputted into the model, we would be able to solve for the $\beta$ with zero errors. However, this is seldom the case, and in practice, there would be fewer hidden layer nodes than $N$. Thus, $\beta$ could be solved with the Moore-Penrose generalized inverse instead for the smallest norm least-squares solution.
\begin{equation}
\hat{\beta} = H^{\dagger} T
\end{equation}

\noindent The final ELM algorithm would then be:
\begin{enumerate}
\item Randomly assign input weights $w_i$ and bias $b_i$ for $i = 1, \ldots, N$.
\item Calculate the hidden layer output matrix $H$
\item Calculate the output weight vector $\beta$
\end{enumerate}

\noindent This replaces gradient descent with a one-shot least-squares solution, allowing orders-of-magnitude faster convergence while avoiding vanishing gradients and local minima. \\

\noindent We plan on replicating the design and investigating various design choices, like:
\begin{enumerate}
\item Whether the weight distribution is more normal
\item Various activation functions, differentiable and non-differentiable
\item Including a small regularization term
\end{enumerate}

\section{Experiments}
Some noteworthy experiments run by the authors experiment with the ERM against more traditional models like a multi-layered neural network (NN) with back propagation (BP) and support vector machines (SVM). The mentioned specifications of the benchmarking environment are:
\begin{itemize}
\item PC with Pentium 4 processor, 1.9 GHz CPU running MATLAB and C
\item Levenberg–Marquardt back propagation with a sigmoidal activation function
\item Radial Basis Kernel for SVM
\end{itemize}

Some of the hyperparameters, like the SVM's C value, were adjusted at each experiment and assumed to be tuned. However, the author's choice of NNs with BP and SVMs is presumably due to their simplicity, as other techniques would introduce new parameters that would make it difficult to compare the actual impact of SLFN without BP. The experiments for this project will remain with NNs with BP and SVMs as a baseline; however, an investigation into the various types of NNs and SVMs is expected. \\

The metrics measured for comparison in these benchmarks are mainly output accuracy via root mean square error (RMSE), success rate (unknown metric), and training/testing time. We plan to test the model with classification problems and measure additional metrics like F1-score and ROC curve. \\

One of the many test benches was the approximation of the SinC function with noise. This could easily be replicated with various other functions to stress test the ELM's ability to approximate complex nonlinear functions with only a single layer, rather than relying on the guessing and adjustment of multiple layers. \\

As mentioned, the proposed ELM model was tested against real-world datasets in both regression and classification domains. To validate the promising results, we will be running against other, more difficult datasets that may not be selectively picked, but not ones that require much preprocessing, as that would be too time-consuming to balance with hyperparameter tuning. \\

Alongside experimenting with the performance of the model, various experiments on the design of the algorithm could be modified to test the claims of the paper. One of which would be investigating $L$, the number of nodes in the hidden layer, and whether it could produce a zero error test result when $L$ is equal to $N$, the input size. Additionally, testing the generalization of the model by observing the weight distribution and whether more normal means less specialization. \\

\section{Results}
The main metrics we will be measuring are test set accuracy, training time, and model size, as well as convergence time and standard deviation. \\

The first experiment run was the artificially created dataset for a basic test between ELM, BP, and SVM. Here, the authors artificially synthesized a dataset with 5000 samples of a SinC function uniformly distributed across an interval of $(-10, 10)$ and included noise between $[-0.2, 0.2]$ to make it more realistic. \\

\begin{figure}[h]
\centering
\includegraphics[width=1\textwidth]{figure1.png}
\caption{Outputs of the ELM learning algorithm}
\label{fig:elm_output}
\end{figure}

From Figure \ref{fig:elm_output}, we can see that the ELM was able to closely follow the patterns from the data sample to approximate a curve quite similar to the expected curve. This was done with only 20 hidden nodes compared to the 5000 data samples. From the assumption that it would require the same number of hidden layer nodes as input size to get no errors, a hidden layer with only 95\% of the input size gave an impressive accuracy illustrated in Table \ref{tab:sinc_res}.

\begin{table}[h]
\centering
\caption{Performance results of SinC approximation}
\label{tab:sinc_res}
\begin{tabular}{@{}lcccccc@{}}
\hline
Algorithms & \multicolumn{2}{c}{Time(s)} & \multicolumn{2}{c}{Training} & \multicolumn{2}{c}{Testing}\\
\cline{2-3} \cline{4-5} \cline{6-7}
 & Training & Testing & RMS & Dev & RMS & Dev \\
\hline
ELM & 0.125 & 0.031 & 0.1148 & 0.0037 & 0.0097 & 0.0028\\
BP & 21.26 & 0.032 & 0.1196 & 0.0042 & 0.0159 & 0.0041\\
SVR & 1273.4 & 5.9087 & 0.1149 & 0.0007 & 0.0130 & 0.0012\\
\hline
\end{tabular}
\end{table}

Table \ref{tab:sinc_res} shows the average results over 50 trials with the standard deviation. From the tables, we can see that overall, the ELM's performance time is quite similar to the BP's but slightly better. In terms of training time, we can see that ELM only takes 0.125 seconds to train while BP takes 21.26 seconds, a staggering 170 times higher training time. Additionally, the SVM has a much larger training and testing time, being over 10,000 times slower, only to receive similar results.

To test the algorithm on more practical and realistic problems, the performance of ELM, BP, and SVMs was compared on publicly available datasets with unknown distributions and noisy data. Cross-validation was used to find the best number of nodes in the hidden layer, and the average results are aggregated and reported in Tables \ref{tab:elm_bp} and \ref{tab:elm_svr}.

\begin{table}[h]
\centering
\caption{Comparison of training/testing RSME of BP and ELM}
\label{tab:elm_bp}
\begin{tabular}{@{}lcccc@{}}
\hline
\textbf{Data sets} & \multicolumn{2}{c}{\textbf{BP}} & \multicolumn{2}{c}{\textbf{ELM}} \\
\cline{2-3} \cline{4-5}
 & Training & Testing & Training & Testing \\
\hline
Abalone & 0.0785 & 0.0874 & 0.0803 & \textbf{0.0824} \\
Delta ailerons & 0.0409 & 0.0481 & 0.0423 & \textbf{0.0431} \\
Delta elevators & 0.0544 & 0.0592 & 0.0550 & 0.0568 \\
Computer activity & 0.0273 & 0.0409 & 0.0316 & 0.0382 \\
Census (house8L) & 0.0596 & 0.0685 & 0.0624 & 0.0660 \\
Auto price & 0.0443 & 0.1157 & 0.0754 & \textbf{0.0994} \\
Triazines & 0.1438 & 0.2197 & 0.1897 & \textbf{0.2002} \\
Machine CPU & 0.0352 & 0.0826 & 0.0332 & \textbf{0.0539} \\
Servo & 0.0794 & 0.1276 & 0.0707 & \textbf{0.1196} \\
Breast cancer & 0.2788 & 0.3155 & 0.2470 & \textbf{0.2679} \\
Bank domains & 0.0342 & 0.0379 & 0.0406 & 0.0366 \\
California housing & 0.1046 & 0.1285 & 0.1217 & 0.1267 \\
Stocks domain & 0.0179 & 0.0358 & 0.0251 & 0.0348 \\
\hline
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Comparison of training/testing RMSE for SVR and ELM}
\label{tab:elm_svr}
\begin{tabular}{@{}lcccc@{}}
\hline
\textbf{Data sets} & \multicolumn{2}{c}{\textbf{SVR}} & \multicolumn{2}{c}{\textbf{ELM}} \\
\cline{2-3} \cline{4-5}
 & Training & Testing & Training & Testing \\
\hline
Abalone & 0.0759 & 0.0784 & 0.0803 & 0.0824 \\
Delta ailerons & 0.0418 & 0.0429 & 0.0423 & 0.0431 \\
Delta elevators & 0.0534 & 0.0540 & 0.0545 & 0.0568 \\
Computer activity & 0.0464 & 0.0470 & 0.0316 & \textbf{0.0382} \\
Census (house8L) & 0.0718 & 0.0746 & 0.0624 & \textbf{0.0660} \\
Auto price & 0.0652 & 0.0937 & \textbf{0.0754} & 0.0994 \\
Triazines & 0.1432 & 0.1829 & \textbf{0.1897} & 0.2002 \\
Machine CPU & 0.0574 & 0.0811 & 0.0332 & \textbf{0.0539} \\
Servo & 0.0840 & 0.1177 & 0.0707 & 0.1196 \\
Breast cancer & 0.2278 & 0.2643 & 0.2470 & 0.2679 \\
Bank domains & 0.0454 & 0.0467 & 0.0406 & \textbf{0.0366} \\
California housing & 0.1089 & 0.1180 & \textbf{0.1217} & 0.1267 \\
Stocks domain & 0.0503 & 0.0518 & 0.0251 & \textbf{0.0348} \\
\hline
\end{tabular}
\end{table}

The measured RSMEs of the models with a difference greater than 0.005 are bolded to show the better-performing model. From Table \ref{tab:elm_bp}, the ELM noticeably performs better than BP, with all the testing RSM errors proving to be lower. On the other hand, Table \ref{tab:elm_svr} shows that the SVR does outperform the ELM in several datasets, roughly having the same performance.

\begin{table}[h]
\centering
\caption{Training and Testing Times (in seconds) for BP, SVR, and ELM}
\label{tab:training_times}
\begin{tabular}{@{}lcccccc@{}}
\hline
\textbf{Data sets} & \multicolumn{2}{c}{\textbf{BP (s)}} & \multicolumn{2}{c}{\textbf{SVR (s)}} & \multicolumn{2}{c}{\textbf{ELM (s)}} \\
\cline{2-3} \cline{4-5} \cline{6-7}
 & Training & Testing & Training & Testing & Training & Testing \\
\hline
Abalone & 1.7562 & 0.0063 & 1.6123 & 0.3223 & 0.0125 & 0.0297 \\
Delta ailerons & 2.7525 & 0.0156 & 0.6726 & 0.2231 & 0.0591 & 0.0627 \\
Delta elevators & 1.1938 & 0.0125 & 1.1210 & 0.5868 & 0.2812 & 0.2047 \\
Computer activity & 67.4400 & 0.0688 & 1.0149 & 0.3027 & 0.2951 & 0.1720 \\
Census (house8L) & 8.0647 & 0.0457 & 11.2510 & 4.1469 & 1.0795 & 0.6298 \\
Auto price & 0.2456 & $<10^{-4}$ & 0.0042 & 0.0465 & 0.0016 & $<10^{-4}$ \\
Triazines & 0.5484 & $<10^{-4}$ & 0.0086 & 0.0540 & $<10^{-4}$ & $<10^{-4}$ \\
Machine CPU & 0.2354 & $<10^{-4}$ & 0.0018 & 0.0421 & 0.0015 & $<10^{-4}$ \\
Servo & 0.2447 & $<10^{-4}$ & 0.0045 & 0.0394 & $<10^{-4}$ & $<10^{-4}$ \\
Breast cancer & 0.3856 & $<10^{-4}$ & 0.0064 & 0.0591 & $<10^{-4}$ & $<10^{-4}$ \\
Bank domains & 7.5060 & 0.0466 & 1.6084 & 0.3021 & 0.6434 & 0.2205 \\
California housing & 6.5320 & 0.0469 & 74.1840 & 10.4530 & 1.1177 & 0.3033 \\
Stocks domain & 1.0487 & 0.0063 & 0.0690 & 0.0637 & 0.0172 & 0.0297 \\
\hline
\end{tabular}
\end{table}


Despite using real datasets, Table \ref{tab:training_times} shows that the ELM offers comparable accuracy to the baseline but drastically reduced training time, which backs the claims of the author. These baseline results from the original work establish a benchmark we aim to reproduce and extend, verifying consistency using modern libraries and datasets.

\begin{figure}[h]
\centering
\includegraphics[width=1\textwidth]{figure2.png}
\caption{Outputs of the ELM learning algorithm}
\label{fig:hidden_nodes}
\end{figure}

\section{Effect of Hidden-Layer Width on Generalization}

Figure~\ref{fig:hidden_nodes} shows the trend between the number of hidden nodes in the ELM and its generalization performance on the California Housing dataset. As observed, the model maintains stable performance across a broad range of hidden-layer sizes, showing a clear region where increasing the number of nodes does not significantly impact accuracy. However, performance tends to degrade when the network is either too small, leading to underfitting, or excessively large, which can introduce overfitting and numerical instability in the output weight computation.

In practice, this implies that the exact number of hidden nodes in ELM does not need to be heavily tuned. Selecting a width within the observed stable region typically gets near-optimal performance, which supports the authors' claims of low error with fewer hidden nodes. This reinforces one of ELM’s main advantages, being robust and efficient generalization without the need for extensive hyperparameter optimization.

\section{Timeline}

\begin{table}[h]
\centering
\caption{Proposed timeline}
\label{tab:timeline}
\begin{tabular}{@{}p{3cm}p{10cm}@{}}
\hline
Date Range & Task \\
\hline
Oct 21–Oct 28 & Understand the original ELM paper thoroughly; set up coding environment; synthesize dataset. \\
Oct 29–Nov 10 & Implement ELM algorithm; test on small dataset; debug. \\
Nov 11–Nov 25 & Implement baseline FNN; run initial comparisons for hidden-node counts; verify author's results on real-world datasets \\
% Nov 26–Dec 10 & Full parameter sweep: hidden nodes (\{100,500,1000,5000\}), activation functions, regularization; collect results; perform statistical tests. \\
Dec 11–Dec 20 & Analyze results; write up results section; prepare presentation/visuals; finalize report. \\
Dec 20 & Buffer for unexpected delays; finalize deliverables; \\
\hline
\end{tabular}
\end{table}

Table \ref{tab:timeline} lays out a timeline of deliverables to be completed after October 21 until the end of the semester. If there are unexpected difficulties building the model or computing the final output layer, a minimum product will be an ELM implementation and validation against the synthesized datasets.

\section{Conclusion}
Within this report, we explored the paper presented by Huang \cite{huang2006extreme} and this team, experimenting with the design of an Extreme Learning Machine with its architecture as an SLFN without BP. This project will work to deliver a working implementation of the ELM algorithm, comparison against a conventional neural network baseline, and an analysis of design choices impacting speed and accuracy. If completion of full parameter sweeps is not possible within the time frame, the minimum deliverable will still consist of a functional ELM implementation, baseline comparison, and a discussion of preliminary findings and potential extensions for future work. This work will help deepen the understanding of training-speed vs generalization trade-offs in neural network design, especially in the context of randomly-initialized hidden layers and analytic output solves.

\newpage
\bibliographystyle{plain}
\bibliography{refs}

\end{document}